name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build EXE
        run: pyinstaller --noconfirm --clean Via_ProMax.spec

      - name: Zip release
        shell: pwsh
        run: Compress-Archive -Path dist/Via_ProMax/* -DestinationPath Via_ProMax-Windows.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Via_ProMax-Windows
          path: Via_ProMax-Windows.zip

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: Via_ProMax-Windows.zip
